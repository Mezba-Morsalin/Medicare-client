import React from 'react';
import SignUpPage from './SignUp';

export const metadata = {
    title : "MediCare Connect - Sign Up",
    description : "Signup"
}

const page = () => {
  return (
    <div>
      <SignUpPage/>
    </div>
  );
};

export default page;